from django.shortcuts import render
from django.http import HttpResponse

# Create your views here.

#def hola(request):
	#return HttpResponse("Hola")
	
def index(request):
    return render(request, 'socialcookies/index.html')
